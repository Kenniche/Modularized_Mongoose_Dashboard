<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard Page</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" 
    integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel='stylesheet' type='text/css' href='stylesheet.css'>
</head>
<body>
    <br>
    <div class='container container d-flex justify-content-center'>
        <div id='wrapper' class="card shadow p-3 mb-5 bg-light rounded">
            
            <center><h3>Animal Tracker</h3></center>
            <% if(typeof(fishData) != 'undefined' ) { %>
                <h3>Here are the awesome Fish!</h3>
                <% for (var x in fishData) { %>
                    <a href="/fishes/<%= fishData[x]._id %>"><h4>Fish Name: <%= fishData[x].name %></h4></a>

                    <div class="btn-group" role="group" aria-label="Basic mixed styles example">
                    <a href="/fishes/edit/<%= fishData[x]._id %>" class="btn btn-outline-primary" >Edit</a>
                    <form action="/fishes/destroy/<%= fishData[x]._id %>" method="post">
                        <input type='submit'class="btn btn-outline-danger"  value='Delete This Fish'>
                            </div>
                    </form>
                    
                    <!-- <h4>Fish Length: <%= fishData[x].length %></h4> -->
                <hr>
                <% } %>
            <% } %>
            <div><a href="/fishes" class="btn btn-outline-success" >Create a new Fish!</a></div>
            
        </div>
    
        <div>
            <% if(typeof(errors) != 'undefined' ) { %>
                <h3>Here are the un-awesome errors!</h3>
                <% for (var x in errors) { %>
                <h3><%= errors[x].message %></h3>
                <% } %>
            <% } %>
        </div>
    </div>
</body>
</html>